machine:
  environment:
    USER_NAME: mszalbach
    USER_EMAIL: 4-bar-killa@web.de
  java:
    version: oraclejdk8

dependencies:
  override:
    - mvn --fail-never dependency:go-offline || true

test:
  override:
    - mvn package -B
    
deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "$USER_EMAIL"
      - git config --global user.name "$USER_NAME"
      - ./myarn deploy
