machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - mvn --fail-never dependency:go-offline || true

test:
  override:
    - mvn package -B
    
deployment:
  production:
    branch: master
    commands:
      - ./myarn deploy
